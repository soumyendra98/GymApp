import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#EB3A02",
    },
    secondary: {
      main: "#247B37",
    },
    info: {
      main: "#363333",
    },
  },
});

export default theme;
